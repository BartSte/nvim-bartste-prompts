return {
  abort = require("prompts.commands.abort"),
  ask = require("prompts.commands.ask"),
  commit = require("prompts.commands.commit"),
  edit = require("prompts.commands.edit"),
  is_running = require("prompts.commands.is_running"),
  output = require("prompts.commands.output"),
  show_output = require("prompts.commands.show_output"),
  undo = require("prompts.commands.undo"),
}
